.. include:: sub.txt

=====================
MultilinearBackbone
=====================

.. function:: hystereticBackbone('Multilinear', backboneTag, e1, s1, e2, s2, ...)
   :noindex:

   The backbone function is trilinear defined by any number of controlling points
   :math:`(e_1, s_1)`, :math:`(e_2, s_2)`, ...

   ===================================   ===========================================================================
   ``backboneTag`` |int|                 integer tag identifying the backbone function.
   ``e1`` |float|                        parameter :math:`e_1`.
   ``s1`` |float|                        parameter :math:`s_1`.
   ``e2`` |float|                        parameter :math:`e_2`.
   ``s2`` |float|                        parameter :math:`s_2`.
   ===================================   ===========================================================================

